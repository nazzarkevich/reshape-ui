// eslint-disable-next-line no-unused-vars
const basic = styles => ({
    cursor:     'default',
    display:    'flex',
    alignItems: 'center',
    position:   'relative'
});

const theme = (styles) => ({
    ...basic(styles)
});

export default theme;


