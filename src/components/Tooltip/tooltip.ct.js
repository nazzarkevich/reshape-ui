import styles from '../styles';

const PLACEMENT = {
    TOP:       'top',
    TOP_START: 'top-start',
    TOP_END:   'top-end',
    RIGHT:     'right',
    BOTTOM:    'bottom',
    LEFT:      'left'

};

const ENTER_DELAY = 0;
const INDENT = styles.gutter;
const VIEWPORT_SPACE = styles.gutter;
const DEFAULT_TITLE = 'your title';
const TOOLTIP_SCROLL_CONTAINER = 'tooltip_scroll_container';

export {
    INDENT,
    PLACEMENT,
    ENTER_DELAY,
    DEFAULT_TITLE,
    VIEWPORT_SPACE,
    TOOLTIP_SCROLL_CONTAINER
};
