const scrollIntoViewOptions = {
    behavior: 'instant',
    block:    'start',
    inline:   'start'
};

const REASON = {
    SELECT: 'select-option',
    CREATE: 'create-option',
    REMOVE: 'remove-option'
};

export { REASON, scrollIntoViewOptions };
